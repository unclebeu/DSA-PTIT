#include<bits/stdc++.h>
using namespace std;
using ll=long long;
int n;
std::vector<int> adj(100005);
int main()
{
   ios_base::sync_with_stdio(false);cin.tie(NULL);
   cin>>n;while (n--){
   		int x,y;
   		cin>>x>>y;
   		adj[x].push_back(y);
   		adj[y].push_back(X);
   }
   

}  